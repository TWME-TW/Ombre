package dev.twme.ombre.blockcolors.data;

/**
 * 方塊類別枚舉
 */
public enum BlockCategory {
    BUILDING,      // 建築方塊 (is_decoration = false)
    DECORATION,    // 裝飾方塊 (is_decoration = true)
    ALL            // 所有方塊
}
